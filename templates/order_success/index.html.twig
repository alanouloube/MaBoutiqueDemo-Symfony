{% extends 'base.html.twig' %}

{% block title %}Confirmation de commande
{% endblock %}

{% block content %}
	<h2>Confirmation de commande</h2>
	<p>
		Bonjour
		<strong>{{ order.user.firstname }}
			{{ order.user.lastname }}</strong>,<br/><br/>
		Nous vous remercions pour votre commande n°<strong>{{ order.reference }}</strong>.<br/>
		Une confirmation vient de vous être envoyée par email à l'adresse
		<strong>{{ order.user.email }}</strong>.
		<hr>
		Votre commande sera livrée par
		<strong>{{ order.carrierName }}</strong>
		à l'adresse suivante :
		<br/>
		<div class="form-check mt-4">
			{{ order.delivery|raw }}
		</div>
		<hr>
		<div class="pt-5">
			Pour suivre votre commande, rendez-vous dans votre
			<a class="btn btn-dark" href="{{ path('account_order') }}">compte</a>
		</div>
	</p>
{% endblock %}
